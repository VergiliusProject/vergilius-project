extends /partials/page

block title
    title Vergilius Project
    -var selected = 'Family';
block content
    .container
        .cmd
            ul.list-inline.breadcrumb(type='none')
                li.list-inline-item
                    | >
                    a(href='/' tabindex="-1")  Home
                li.list-inline-item
                    a(href='/kernels' tabindex="-1") Kernels
                li.list-inline-item #{famname}
    .container.indent
        .mainblock
            p(style='text-align: left; font-size: 33px; font-weight: 600') #{famname}
            div
                ul(type='none')
                    for i in fam
                        li: p
                            a(href="/os/#{i.osname}" class='listos' tabindex="-1") #{i.idopersys} #{i.osname}